# 更新日志

本文件记录 War3 Rect Edit 的版本变更。VS Code Marketplace 的 **Changelog** 标签页会直接读取此文件，因此请随每个正式版本一起更新。

## 0.1.23

### 修复

- 修复扩展在 VS Code Marketplace 上的简介显示为乱码的问题。0.1.22 的简介在商店索引中被错误地存成了一串 `?`，本版重新发布以重建该信息。功能与 0.1.22 相同。

## 0.1.22

### 新增

- 选中的点位或区域若不在右侧列表的可视区内，列表会自动滚动把它带进视野，滚动带 0.5 秒缓动、目标位置居中。
- 自动滚动会让位于手动操作：手动滚动列表或按住列表拖动时，正在进行的滚动动画会立即中断。拖动对象或框选的过程中不会反复触发滚动，只在选中结果确定后滚动一次。

### 变更

- 右侧列表改为按对象复用行：选中对象只更新高亮，不再整表重建。对象数量较多时刷新更平滑，被点击的行也不会丢失键盘焦点。

### 修复

- 修复每次选中点位或区域都会把右侧列表滚动条重置回顶部的问题。现在只有显式切换「区域 / 点位」标签页才会回到列表顶部。

## 0.1.21

### 新增

- 多选对象粘贴时可先选择 **实例** 或 **普通**：实例粘贴会把副本与源对象绑定，之后移动任意一侧，另一侧都会跟着移动，两组的相对位置保持不变。
- 粘贴时可勾选 **镜像**，方向可选 `左右` 或 `上下`；副本按该方向翻转，实例联动时的移动方向也随之镜像。
- 选中的对象若存在联动副本，副本会用青色虚线圈标出，便于定位。
- 对带实例关联的对象单击右键（不拖动）会弹出菜单，可 **解除实例关联**；链接断开后对象留在原位，操作可用 `Ctrl+Z` 撤销。

### 变更

- 粘贴预览状态下，右键恢复为纯粹的镜头操作（平移，配合 `Ctrl` 为旋转），**不再取消粘贴**；取消放置请使用 `Esc`。

### 修复

- 修复镜像后的区域实例在重新打开地图后拖动会跳位的问题：实例关联的偏移量改为按镜像后的角落锚点推导，左右镜像下不再偏差一个矩形宽度。

### 存储

- 实例关联保存在工作区根目录的 `.war3tool/instances.json`，与地图和脚本读取的文件完全分离。
- 该文件只记录拓扑关系（谁是谁的副本、是否镜像），**不记录坐标**，坐标仍以 `points.json` 与 `war3map.w3r` 为唯一来源。
- `points.json`、`points.lua`、`war3map.w3r` 的格式未做任何改动。

## 0.1.20

### 新增

- `Ctrl+C` / `Ctrl+V` 复制粘贴选中的点或区域，粘贴时显示半透明预览，移动鼠标后单击放置。
- `Ctrl+Z` 撤销、`Ctrl+Y` 前进，最多保留 10 步历史。
- 点和区域吸附：支持吸附开关与吸附距离设置，吸附时显示辅助线，并沿对齐虚线标注对象间距（码数标尺）。
